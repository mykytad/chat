<li>
  <div>
    <p class="text-gray-700">
      <%= notifier.to_notifier.message %>
    </p>
    <div class="flex justify-between mt-1 text-gray-500 text-sm space-x-4">
      <p>
        Received on <%= notifier.created_at.to_date %>
      </p>
      <p>
        Status: <%= notifier.read? ? "Read" : "Unread" %>
      </p>
    </div>
  </div>
</li>